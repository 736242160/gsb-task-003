我想用 Python 做一个零外部依赖的轻量级异步服务网格（Service Mesh）流量治理与熔断降级仿真内核，并在当前目录下交付一个开箱即用的原生可视化控制台。代码必须保持纯原生实现，严禁引入任何第三方包或外部依赖（仅允许使用 Python 官方标准库如 http.server、asyncio、threading、time、json、unittest 等；前端页面也只能使用纯原生 HTML、CSS 和 JavaScript，不要使用任何 CDN 脚本或样式库）。
这个调度内核需要仿真多节点微服务集群在面对突发并发和网络故障时的流量控制。你需要完整实现基于动态权重的平滑负载均衡，以及断路器的三态状态机（关闭、开启、半开启）。在正常状态下，流量根据节点延迟与健康评分平滑分发；当某个节点连续超时或错误率达到阈值时，必须立即熔断进入开启状态，对后续请求执行快速失败拦截，防止级联雪崩；在静默窗口结束后，断路器自动步入半开启状态并放行小比例试探流量，连续成功则恢复正常，一旦再次异常则重置冷却时间。所有状态流转、并发计数与路由分发必须保证绝对的线程安全，杜绝死锁与数据竞态。
服务启动后需要监听本地端口并提供直观的原生 Web 界面，用纯原生 Canvas 或 DOM 连线动态绘制各服务节点的拓扑网络。页面上需要清晰展示节点的健康度、断路器当前状态变色以及实时吞吐。同时提供一键模拟演示按钮，至少包含正常突发限流分流、以及对特定节点注入延迟引发熔断、流量切除并在故障恢复后半开自愈的全过程，点击后能在页面上实时动态看到拓扑链路重定向与节点变色流转，并同步滚动展示时序治理日志。
同时提供一个开箱即用的主入口，运行后既能执行测试，又能启动本地服务方便在浏览器中直接查看可视化效果。
